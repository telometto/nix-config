# Deprecated: legacy aggregate device configurations retained temporarily.
# All hosts now import a dedicated module in devices/*.nix.
# This file can be removed once confirmed unused.
{ config, lib, pkgs, VARS, ... }: { }
