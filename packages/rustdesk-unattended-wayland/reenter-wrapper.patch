diff --git a/src/platform/linux.rs b/src/platform/linux.rs
--- a/src/platform/linux.rs
+++ b/src/platform/linux.rs
@@ -1415,7 +1415,9 @@ where
         Some(id_name) => id_name,
         None => get_active_user_id_name(),
     };
-    let cmd = std::env::current_exe()?;
+    // sudo clears loader variables: re-enter the Nix wrapper for the session.
+    // The wrapper execs the same ELF, preserving the DRM IPC executable check.
+    let cmd = PathBuf::from("@rustdesk-wrapper@");
     if uid.is_empty() {
         bail!("No valid uid");
     }
