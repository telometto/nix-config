# KubeVirt Migration — Plan 1: Cluster Bootstrap

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up k3s on blizzard with KubeVirt, CDI, Cilium, MetalLB, Sealed Secrets, gVisor, Traefik, and Flux all running and healthy — with MicroVMs still running untouched.

**Architecture:** nix-config gains Cilium-compatible k3s flags and gVisor runtime config, then disables NixOS-managed Traefik and cloudflared in the same `nixos-rebuild boot` so the cutover is atomic on reboot. A new `homelab-apps` git repo holds all Kubernetes manifests; Flux reconciles it to the cluster. This is Plan 1 of 3 — Plans 2 (container services) and 3 (VM migration + cleanup) follow.

**Tech Stack:** NixOS, k3s 1.35.2+k3s1, KubeVirt v1.3.1, CDI v1.60.2, Cilium v1.19.3, MetalLB v0.14, Sealed Secrets v2.18.5, gVisor (runsc), Traefik v3, Flux v2, Helm 3, kubectl, kubeseal, flux CLI

**⚠ Downtime note:** Disabling NixOS Traefik in Task 3 takes down all current web services (lingarr, dashboard, metrics, scrutiny, all MicroVM-routed subdomains) until Traefik is re-deployed as a k3s workload in Task 14. CrowdSec bouncer loses its Traefik integration during this window — the CrowdSec service itself keeps running. This is acceptable per the stated tolerance for downtime.

______________________________________________________________________

## File Map

**Modified in nix-config:**

- `modules/services/k3s.nix` — add Cilium/MetalLB-compatible flags, KVM module
- `hosts/blizzard/security/traefik.nix` — disable `services.traefik`
- `hosts/blizzard/services/cloudflared.nix` — disable `sys.services.cloudflared`

**Created in nix-config:**

- `hosts/blizzard/virtualisation/gvisor.nix` — gVisor package + containerd template systemd service

**Created in homelab-apps (new repo):**

- `flux/gotk-components.yaml` — Flux CRDs and controllers (generated by flux CLI)
- `flux/gotk-sync.yaml` — Flux GitRepository + Kustomization pointing at homelab-apps
- `namespaces/namespaces.yaml` — all namespaces with Pod Security Standard labels
- `network/cilium-helmrepo.yaml` — Cilium Helm repository
- `network/cilium-helmrelease.yaml` — Cilium HelmRelease
- `network/metallb-helmrepo.yaml` — MetalLB Helm repository
- `network/metallb-helmrelease.yaml` — MetalLB HelmRelease
- `network/metallb-ippool.yaml` — IPAddressPool + L2Advertisement
- `network/default-deny.yaml` — default-deny NetworkPolicies for all namespaces
- `kubevirt/kubevirt-operator.yaml` — KubeVirt operator manifest
- `kubevirt/kubevirt-cr.yaml` — KubeVirt CR (enables QEMU/KVM)
- `kubevirt/runtimeclass-gvisor.yaml` — gVisor RuntimeClass
- `cdi/cdi-operator.yaml` — CDI operator manifest
- `cdi/cdi-cr.yaml` — CDI CR
- `sealed-secrets/helmrepo.yaml` — Sealed Secrets Helm repository
- `sealed-secrets/helmrelease.yaml` — Sealed Secrets HelmRelease
- `ingress/traefik-helmrepo.yaml` — Traefik Helm repository
- `ingress/traefik-helmrelease.yaml` — Traefik HelmRelease
- `ingress/cloudflared-deployment.yaml` — cloudflared Deployment
- `ingress/cloudflared-secret-sealed.yaml` — SealedSecret for tunnel credentials

______________________________________________________________________

## Task 1: Update k3s NixOS module for Cilium + MetalLB

**Files:**

- Modify: `modules/services/k3s.nix`

- Modify: `hosts/blizzard/blizzard.nix`

- [ ] **Step 1: Update k3s extraFlags default**

Open `modules/services/k3s.nix`. Replace the `extraFlags` default:

```nix
extraFlags = lib.mkOption {
  type = lib.types.listOf lib.types.str;
  default = [
    "--snapshotter=native"
    "--disable=traefik"
    "--disable=servicelb"
    "--flannel-backend=none"
    "--disable-network-policy"
    "--kubelet-arg=read-only-port=10255"
  ];
};
```

`--disable=servicelb` removes k3s's built-in ServiceLB (MetalLB replaces it). `--flannel-backend=none` and `--disable-network-policy` hand CNI and policy enforcement to Cilium.

- [ ] **Step 2: Enable k3s and load KVM kernel module on blizzard**

Open `hosts/blizzard/blizzard.nix`. Change `k3s.enable = false` to `true` and add the KVM module. Check `/proc/cpuinfo` for `vendor_id`: GenuineIntel → `kvm-intel`, AuthenticAMD → `kvm-amd`.

```nix
boot.kernelModules = [ "kvm-intel" ];   # or "kvm-amd" — check /proc/cpuinfo

sys.services.k3s.enable = true;
```

- [ ] **Step 3: Verify nix config evaluates**

```bash
nix build .#nixosConfigurations.blizzard.config.system.build.toplevel --no-link 2>&1 | tail -5
```

Expected: build completes with no errors. Fix any evaluation errors before continuing.

- [ ] **Step 4: Commit**

```bash
git add modules/services/k3s.nix hosts/blizzard/blizzard.nix
git commit -m "feat(blizzard): enable k3s with Cilium-compatible flags and KVM"
```

______________________________________________________________________

## Task 2: Add gVisor runtime config to blizzard

**Files:**

- Create: `hosts/blizzard/virtualisation/gvisor.nix`

- [ ] **Step 1: Create gvisor.nix**

Create `hosts/blizzard/virtualisation/gvisor.nix`:

```nix
{ pkgs, ... }:
{
  # Install gVisor (runsc) — k3s's containerd uses it as an additional runtime class
  environment.systemPackages = [ pkgs.gvisor ];

  # Write the containerd config template before k3s starts so it picks up runsc
  # k3s reads this template at /var/lib/rancher/k3s/agent/etc/containerd/config.toml.tmpl
  systemd.services.k3s-gvisor-containerd-config = {
    description = "Write gVisor containerd runtime config for k3s";
    wantedBy = [ "k3s.service" ];
    before = [ "k3s.service" ];
    serviceConfig = {
      Type = "oneshot";
      RemainAfterExit = true;
    };
    script = ''
      mkdir -p /var/lib/rancher/k3s/agent/etc/containerd
      cat > /var/lib/rancher/k3s/agent/etc/containerd/config.toml.tmpl << 'TMPL'
      version = 2

      [plugins."io.containerd.grpc.v1.cri".containerd]
        snapshotter = "native"
        default_runtime_name = "runc"

      [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc]
        runtime_type = "io.containerd.runc.v2"

      [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runsc]
        runtime_type = "io.containerd.runsc.v1"

      [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runsc.options]
        BinaryName = "${pkgs.gvisor}/bin/runsc"
        TypeUrl = "io.containerd.runsc.v1.options"
      TMPL
    '';
  };
}
```

- [ ] **Step 2: Verify gvisor package exists in nixpkgs**

```bash
nix build nixpkgs#gvisor --no-link 2>&1 | tail -3
```

Expected: build succeeds silently (it's in cache). If it fails, check `nix search nixpkgs gvisor` for the correct attribute name and update the nix file.

- [ ] **Step 3: Verify config evaluates**

```bash
nix build .#nixosConfigurations.blizzard.config.system.build.toplevel --no-link 2>&1 | tail -5
```

Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add hosts/blizzard/virtualisation/gvisor.nix
git commit -m "feat(blizzard): configure gVisor runtime for k3s containerd"
```

______________________________________________________________________

## Task 3: Disable NixOS Traefik and cloudflared

**Files:**

- Modify: `hosts/blizzard/security/traefik.nix`
- Modify: `hosts/blizzard/services/cloudflared.nix`

**⚠ This task commits the service outage. All web services go down on reboot (next task).**

- [ ] **Step 1: Disable NixOS Traefik**

Open `hosts/blizzard/security/traefik.nix`. The file configures `services.traefik` directly. Wrap the entire `services.traefik` block and the `systemd.services.traefik.serviceConfig` block with a `lib.mkIf false` so the file remains intact for reference but produces no config:

```nix
{ lib, config, VARS, ... }:
let
  # ... (keep existing let bindings) ...
in
{
  # NixOS Traefik disabled — replaced by k3s Traefik Deployment in homelab-apps.
  # Keep this file for reference during migration. Remove in Plan 3 cleanup.
}
```

Delete or comment out all `services.traefik`, `systemd.services.traefik.*` stanzas.

- [ ] **Step 2: Disable cloudflared**

Open `hosts/blizzard/services/cloudflared.nix`. Change `enable = true` to `enable = false`:

```nix
{ config, VARS, ... }:
{
  sys.services.cloudflared = {
    enable = false;
    # ... rest of config unchanged (kept for reference)
  };
}
```

- [ ] **Step 3: Verify config evaluates**

```bash
nix build .#nixosConfigurations.blizzard.config.system.build.toplevel --no-link 2>&1 | tail -5
```

Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add hosts/blizzard/security/traefik.nix hosts/blizzard/services/cloudflared.nix
git commit -m "feat(blizzard): disable NixOS Traefik and cloudflared (migrating to k3s)"
```

______________________________________________________________________

## Task 4: Stage config and reboot

- [ ] **Step 1: Stage the new NixOS generation**

```bash
sudo nixos-rebuild boot --flake .#blizzard 2>&1 | tail -10
```

Expected: `building the system configuration...` then `activating the configuration...` and clean exit. The new config is staged for the next boot but not active yet.

- [ ] **Step 2: Verify the staged generation**

```bash
sudo nix-env --list-generations --profile /nix/var/nix/profiles/system | tail -3
```

Expected: the latest generation (highest number) matches the current date/time.

- [ ] **Step 3: Reboot**

```bash
sudo systemctl reboot
```

Wait for the system to come back up (~30–60s depending on hardware).

- [ ] **Step 4: Verify k3s is running**

```bash
sudo systemctl status k3s --no-pager
```

Expected: `active (running)`.

```bash
sudo k3s kubectl get nodes
```

Expected:

```
NAME       STATUS     ROLES                  AGE   VERSION
blizzard   NotReady   control-plane,master   Xm    v1.35.2+k3s1
```

`NotReady` is correct here — Cilium isn't installed yet so the node has no CNI. This is expected.

- [ ] **Step 5: Verify gVisor service ran**

```bash
sudo systemctl status k3s-gvisor-containerd-config --no-pager
```

Expected: `active (exited)` (oneshot service, ran and completed).

```bash
ls -la /var/lib/rancher/k3s/agent/etc/containerd/config.toml.tmpl
```

Expected: file exists.

- [ ] **Step 6: Verify KVM is available**

```bash
ls -la /dev/kvm
```

Expected: `/dev/kvm` exists with `crw-rw----` permissions.

______________________________________________________________________

## Task 5: Create homelab-apps repo

- [ ] **Step 1: Create and initialize the repo**

```bash
mkdir -p ~/homelab-apps
cd ~/homelab-apps
git init
git checkout -b main
```

- [ ] **Step 2: Create directory structure**

```bash
mkdir -p flux namespaces network kubevirt cdi sealed-secrets ingress secrets
touch flux/.gitkeep namespaces/.gitkeep network/.gitkeep kubevirt/.gitkeep \
      cdi/.gitkeep sealed-secrets/.gitkeep ingress/.gitkeep secrets/.gitkeep
```

- [ ] **Step 3: Create .gitignore**

```bash
cat > .gitignore << 'EOF'
*.bak
*.tmp
kubeconfig
EOF
```

- [ ] **Step 4: Initial commit**

```bash
git add .
git commit -m "chore: initialize homelab-apps repo structure"
```

- [ ] **Step 5: Push to a remote**

Create a new private repo on your Gitea instance (e.g., `git.yourdomain.com/zeno/homelab-apps`), then:

```bash
git remote add origin git@ssh-git.yourdomain.com:zeno/homelab-apps.git
git push -u origin main
```

Use the actual domain from `VARS.domains.public` in the SSH URL.

______________________________________________________________________

## Task 6: Bootstrap Flux

- [ ] **Step 1: Install flux CLI on blizzard**

```bash
curl -s https://fluxcd.io/install.sh | sudo bash
flux --version
```

Expected: `flux version 2.x.x`.

Alternatively, if flux is available in nixpkgs on blizzard:

```bash
nix run nixpkgs#fluxcd -- --version
```

- [ ] **Step 2: Export kubeconfig**

```bash
mkdir -p ~/.kube
sudo k3s kubectl config view --raw > ~/.kube/config
chmod 600 ~/.kube/config
kubectl get nodes
```

Expected: node listed (still `NotReady` — Cilium not yet installed).

- [ ] **Step 3: Bootstrap Flux pointing at homelab-apps**

Replace `<token>` with a Gitea personal access token with repo read permission. Replace the URL with the actual Gitea instance URL.

```bash
flux bootstrap git \
  --url=ssh://git@ssh-git.yourdomain.com:2222/zeno/homelab-apps.git \
  --branch=main \
  --path=flux \
  --private-key-file=/home/zeno/.ssh/id_ed25519 \
  --timeout=5m
```

Expected: Flux installs its CRDs and controllers, creates the `flux-system` namespace, and starts reconciling. Output ends with `◎ waiting for Kustomization "flux-system/flux-system" to be reconciled`.

It will fail to fully reconcile (node is `NotReady`), which is expected — Cilium is next.

- [ ] **Step 4: Verify Flux controllers are running**

```bash
kubectl get pods -n flux-system
```

Expected: `helm-controller`, `kustomize-controller`, `notification-controller`, `source-controller` all `Running` (they run on the host network, so they work even without CNI).

- [ ] **Step 5: Commit Flux-generated bootstrap files**

Flux writes `flux/gotk-components.yaml` and `flux/gotk-sync.yaml` to the repo. Pull and commit:

```bash
cd ~/homelab-apps
git pull origin main
git log --oneline -3
```

______________________________________________________________________

## Task 7: Install Cilium

- [ ] **Step 1: Create Cilium HelmRepository**

Create `network/cilium-helmrepo.yaml`:

```yaml
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: cilium
  namespace: flux-system
spec:
  interval: 24h
  url: https://helm.cilium.io
```

- [ ] **Step 2: Create Cilium HelmRelease**

Create `network/cilium-helmrelease.yaml`:

```yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cilium
  namespace: kube-system
spec:
  interval: 30m
  chart:
    spec:
      chart: cilium
      version: "1.16.*"
      sourceRef:
        kind: HelmRepository
        name: cilium
        namespace: flux-system
  values:
    kubeProxyReplacement: true
    k8sServiceHost: "127.0.0.1"
    k8sServicePort: 6443
    ipam:
      mode: kubernetes
    hubble:
      relay:
        enabled: true
      ui:
        enabled: true
    operator:
      replicas: 1
```

`kubeProxyReplacement: true` enables Cilium to replace kube-proxy (k3s uses iptables-based kube-proxy by default; Cilium's replacement is more efficient). `k8sServiceHost/Port` points at the local k3s API server.

- [ ] **Step 3: Create a Flux Kustomization for network manifests**

Create `flux/network.yaml`:

```yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: network
  namespace: flux-system
spec:
  interval: 10m
  path: ./network
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
```

- [ ] **Step 4: Commit and push**

```bash
cd ~/homelab-apps
git add network/cilium-helmrepo.yaml network/cilium-helmrelease.yaml flux/network.yaml
git commit -m "feat: add Cilium CNI via Flux HelmRelease"
git push
```

- [ ] **Step 5: Wait for Flux to reconcile and Cilium to start**

```bash
flux reconcile kustomization flux-system --with-source
kubectl rollout status deployment/cilium-operator -n kube-system --timeout=5m
kubectl get pods -n kube-system -l k8s-app=cilium
```

Expected: Cilium pods in `Running` state on blizzard.

- [ ] **Step 6: Verify node becomes Ready**

```bash
kubectl wait node blizzard --for=condition=Ready --timeout=3m
kubectl get nodes
```

Expected:

```
NAME       STATUS   ROLES                  AGE   VERSION
blizzard   Ready    control-plane,master   Xm    v1.35.2+k3s1
```

______________________________________________________________________

## Task 8: Create all namespaces

- [ ] **Step 1: Create namespaces manifest**

Create `namespaces/namespaces.yaml`:

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: vms
  labels:
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: restricted
---
apiVersion: v1
kind: Namespace
metadata:
  name: media
  labels:
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: restricted
---
apiVersion: v1
kind: Namespace
metadata:
  name: apps
  labels:
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/warn: restricted
---
apiVersion: v1
kind: Namespace
metadata:
  name: internal
  labels:
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: restricted
---
apiVersion: v1
kind: Namespace
metadata:
  name: ingress
  labels:
    pod-security.kubernetes.io/enforce: privileged
```

- [ ] **Step 2: Create Flux Kustomization for namespaces**

Create `flux/namespaces.yaml`:

```yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: namespaces
  namespace: flux-system
spec:
  interval: 10m
  path: ./namespaces
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
```

- [ ] **Step 3: Commit and push**

```bash
cd ~/homelab-apps
git add namespaces/namespaces.yaml flux/namespaces.yaml
git commit -m "feat: create workload namespaces with Pod Security Standards"
git push
```

- [ ] **Step 4: Verify namespaces exist**

```bash
flux reconcile kustomization namespaces --with-source
kubectl get namespaces vms media apps internal ingress
```

Expected: all five namespaces in `Active` state.

______________________________________________________________________

## Task 9: Install MetalLB

- [ ] **Step 1: Create MetalLB HelmRepository**

Create `network/metallb-helmrepo.yaml`:

```yaml
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: metallb
  namespace: flux-system
spec:
  interval: 24h
  url: https://metallb.github.io/metallb
```

- [ ] **Step 2: Create MetalLB HelmRelease**

Create `network/metallb-helmrelease.yaml`:

```yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: metallb
  namespace: metallb-system
spec:
  interval: 30m
  chart:
    spec:
      chart: metallb
      version: "0.14.*"
      sourceRef:
        kind: HelmRepository
        name: metallb
        namespace: flux-system
  install:
    createNamespace: true
  values:
    speaker:
      frr:
        enabled: false
```

- [ ] **Step 3: Configure IP pool for wireguard VIP**

Before creating this file, choose one unused IP from your LAN (`192.168.2.0/24`) that is outside your router's DHCP range. This will be the wireguard VM's LAN-visible IP. Example: `192.168.2.200`.

Create `network/metallb-ippool.yaml`:

```yaml
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: homelab-pool
  namespace: metallb-system
spec:
  addresses:
    - 192.168.2.200-192.168.2.210
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: homelab-l2
  namespace: metallb-system
spec:
  ipAddressPools:
    - homelab-pool
```

- [ ] **Step 4: Commit and push**

```bash
cd ~/homelab-apps
git add network/metallb-helmrepo.yaml network/metallb-helmrelease.yaml network/metallb-ippool.yaml
git commit -m "feat: add MetalLB L2 with homelab IP pool"
git push
```

- [ ] **Step 5: Verify MetalLB**

```bash
flux reconcile kustomization network --with-source
kubectl wait deployment -n metallb-system controller --for=condition=Available --timeout=3m
kubectl get pods -n metallb-system
```

Expected: `controller` and `speaker` pods in `Running` state.

- [ ] **Step 6: Verify IP pool was created**

```bash
kubectl get ipaddresspools -n metallb-system
```

Expected: `homelab-pool` listed with `AUTO ASSIGN: true`. If you see `no resources found` or a CRD error, MetalLB's CRDs weren't ready when the IP pool applied. Wait 30 seconds and reconcile again:

```bash
flux reconcile kustomization network --with-source
kubectl get ipaddresspools -n metallb-system
```

______________________________________________________________________

## Task 10: Install KubeVirt

- [ ] **Step 1: Download KubeVirt operator manifest**

```bash
KUBEVIRT_VERSION=v1.3.1
curl -Lo ~/homelab-apps/kubevirt/kubevirt-operator.yaml \
  https://github.com/kubevirt/kubevirt/releases/download/${KUBEVIRT_VERSION}/kubevirt-operator.yaml
```

- [ ] **Step 2: Create KubeVirt CR**

Create `kubevirt/kubevirt-cr.yaml`:

```yaml
apiVersion: kubevirt.io/v1
kind: KubeVirt
metadata:
  name: kubevirt
  namespace: kubevirt
spec:
  configuration:
    developerConfiguration:
      featureGates:
        - HotplugVolumes
        - HostDisk
        - Sidecar
    machineType: "q35"
  workloadUpdateStrategy:
    workloadUpdateMethods:
      - LiveMigrate
      - Evict
```

`HotplugVolumes` allows attaching volumes to running VMs. `HostDisk` allows using host paths as VM disks.

- [ ] **Step 3: Create Flux Kustomization for KubeVirt**

Create `flux/kubevirt.yaml`:

```yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kubevirt
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubevirt
  prune: false
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: namespaces
```

`prune: false` prevents Flux from deleting KubeVirt CRDs on reconciliation failures.

- [ ] **Step 4: Commit and push**

```bash
cd ~/homelab-apps
git add kubevirt/ flux/kubevirt.yaml
git commit -m "feat: add KubeVirt operator and CR"
git push
```

- [ ] **Step 5: Wait for KubeVirt to be ready**

```bash
flux reconcile kustomization kubevirt --with-source
kubectl wait -n kubevirt kv kubevirt --for=condition=Available --timeout=10m
```

Expected: `kubevirt.kubevirt.io/kubevirt condition met`.

```bash
kubectl get pods -n kubevirt
```

Expected: `virt-api`, `virt-controller`, `virt-handler` (DaemonSet on blizzard) all `Running`.

- [ ] **Step 6: Verify KVM access from within kubevirt**

```bash
kubectl get nodes -o jsonpath='{.items[*].status.allocatable}' | python3 -m json.tool | grep kvm
```

Expected: `"devices.kubevirt.io/kvm": "1k"` or similar non-zero value. If zero, KVM is not accessible — re-check `boot.kernelModules` in blizzard.nix and that `/dev/kvm` exists.

______________________________________________________________________

## Task 11: Install CDI

- [ ] **Step 1: Download CDI operator manifest**

```bash
CDI_VERSION=v1.60.2
curl -Lo ~/homelab-apps/cdi/cdi-operator.yaml \
  https://github.com/kubevirt/containerized-data-importer/releases/download/${CDI_VERSION}/cdi-operator.yaml
```

- [ ] **Step 2: Create CDI CR**

Create `cdi/cdi-cr.yaml`:

```yaml
apiVersion: cdi.kubevirt.io/v1beta1
kind: CDI
metadata:
  name: cdi
spec:
  config:
    featureGates:
      - HonorWaitForFirstConsumer
  imagePullPolicy: IfNotPresent
```

- [ ] **Step 3: Create Flux Kustomization for CDI**

Create `flux/cdi.yaml`:

```yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cdi
  namespace: flux-system
spec:
  interval: 10m
  path: ./cdi
  prune: false
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: kubevirt
```

- [ ] **Step 4: Commit and push**

```bash
cd ~/homelab-apps
git add cdi/ flux/cdi.yaml
git commit -m "feat: add CDI operator for VM disk image management"
git push
```

- [ ] **Step 5: Verify CDI**

```bash
flux reconcile kustomization cdi --with-source
kubectl wait -n cdi cdi cdi --for=condition=Available --timeout=5m
kubectl get pods -n cdi
```

Expected: `cdi-apiserver`, `cdi-deployment`, `cdi-uploadproxy` all `Running`.

______________________________________________________________________

## Task 12: Install Sealed Secrets and back up controller key

- [ ] **Step 1: Create Sealed Secrets HelmRepository**

Create `sealed-secrets/helmrepo.yaml`:

```yaml
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: sealed-secrets
  namespace: flux-system
spec:
  interval: 24h
  url: https://bitnami-labs.github.io/sealed-secrets
```

- [ ] **Step 2: Create Sealed Secrets HelmRelease**

Create `sealed-secrets/helmrelease.yaml`:

```yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sealed-secrets
  namespace: kube-system
spec:
  interval: 30m
  chart:
    spec:
      chart: sealed-secrets
      version: "2.*"
      sourceRef:
        kind: HelmRepository
        name: sealed-secrets
        namespace: flux-system
  values:
    fullnameOverride: sealed-secrets-controller
```

`fullnameOverride` ensures the controller is named `sealed-secrets-controller`, which `kubeseal` expects by default.

- [ ] **Step 3: Create Flux Kustomization**

Create `flux/sealed-secrets.yaml`:

```yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: sealed-secrets
  namespace: flux-system
spec:
  interval: 10m
  path: ./sealed-secrets
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
```

- [ ] **Step 4: Commit and push**

```bash
cd ~/homelab-apps
git add sealed-secrets/ flux/sealed-secrets.yaml
git commit -m "feat: add Sealed Secrets controller"
git push
```

- [ ] **Step 5: Wait for controller to be ready**

```bash
flux reconcile kustomization sealed-secrets --with-source
kubectl wait deployment -n kube-system sealed-secrets-controller --for=condition=Available --timeout=3m
```

- [ ] **Step 6: Install kubeseal CLI**

```bash
KUBESEAL_VERSION=$(kubectl get deployment sealed-secrets-controller -n kube-system -o jsonpath='{.spec.template.spec.containers[0].image}' | cut -d: -f2)
curl -sL "https://github.com/bitnami-labs/sealed-secrets/releases/download/${KUBESEAL_VERSION}/kubeseal-linux-amd64.tar.gz" | tar xz kubeseal
sudo mv kubeseal /usr/local/bin/
kubeseal --version
```

Expected: `kubeseal version X.X.X`.

- [ ] **Step 7: Back up the controller key (CRITICAL)**

```bash
kubectl get secret -n kube-system \
  -l sealedsecrets.bitnami.com/sealed-secrets-key=active \
  -o yaml > /tmp/sealed-secrets-controller-key.yaml
```

**Store this file securely.** It is the private key that decrypts all your SealedSecrets. If you lose it and the cluster is destroyed, all sealed secrets are unrecoverable. Encrypt it with your existing age key before storing:

```bash
age -r $(cat ~/.ssh/id_ed25519.pub | ssh-to-age) \
  -o ~/sealed-secrets-key-backup.age \
  /tmp/sealed-secrets-controller-key.yaml
rm /tmp/sealed-secrets-controller-key.yaml
```

Store `sealed-secrets-key-backup.age` in the nix-secrets flake or another secure offline location.

______________________________________________________________________

## Task 13: Configure gVisor RuntimeClass

- [ ] **Step 1: Create RuntimeClass manifest**

Create `kubevirt/runtimeclass-gvisor.yaml`:

```yaml
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc
scheduling:
  nodeClassification:
    tolerations:
      - key: "node.kubernetes.io/not-ready"
        operator: "Exists"
```

- [ ] **Step 2: Verify gVisor runtime is registered in containerd**

```bash
sudo crictl info | python3 -m json.tool | grep -A5 runsc
```

Expected output includes `"runsc"` runtime type. If absent, check the gVisor systemd service ran successfully (Task 4 step 5) and the template file exists.

- [ ] **Step 3: Commit and push**

```bash
cd ~/homelab-apps
git add kubevirt/runtimeclass-gvisor.yaml
git commit -m "feat: add gVisor RuntimeClass for sandboxed containers"
git push
```

- [ ] **Step 4: Test gVisor with a throwaway pod**

```bash
kubectl run gvisor-test \
  --image=busybox \
  --restart=Never \
  --overrides='{"spec":{"runtimeClassName":"gvisor"}}' \
  -- uname -r
kubectl wait pod gvisor-test --for=condition=Ready --timeout=60s
kubectl logs gvisor-test
kubectl delete pod gvisor-test
```

Expected: `uname -r` returns a gVisor kernel version string like `4.4.0` (gVisor's synthetic kernel), not the host kernel version.

______________________________________________________________________

## Task 14: Deploy Traefik as k3s workload

- [ ] **Step 1: Create Traefik HelmRepository**

Create `ingress/traefik-helmrepo.yaml`:

```yaml
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: traefik
  namespace: flux-system
spec:
  interval: 24h
  url: https://helm.traefik.io/traefik
```

- [ ] **Step 2: Create Traefik HelmRelease**

Create `ingress/traefik-helmrelease.yaml`:

```yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: ingress
spec:
  interval: 30m
  chart:
    spec:
      chart: traefik
      version: "31.*"
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: flux-system
  values:
    deployment:
      replicas: 1
    service:
      type: ClusterIP
    ports:
      web:
        port: 8000
        expose:
          default: true
        exposedPort: 80
        hostPort: 80
      websecure:
        port: 8443
        expose:
          default: true
        exposedPort: 443
        hostPort: 443
        tls:
          enabled: true
    providers:
      kubernetesCRD:
        enabled: true
        allowCrossNamespaceIngress: true
      kubernetesIngress:
        enabled: true
    api:
      dashboard: true
      insecure: false
    logs:
      access:
        enabled: true
    additionalArguments:
      - "--providers.kubernetesCRD.allowExternalNameServices=true"
    securityContext:
      runAsNonRoot: true
      runAsUser: 65532
      runAsGroup: 65532
```

`hostPort: 80/443` binds the container directly to blizzard's host port 80/443 — the same as the old NixOS Traefik. This works because the `ingress` namespace has `privileged` Pod Security Standard. NodePort would only expose 30080/30443 and require an extra NAT hop.

**Note:** CrowdSec bouncer integration is not in this initial Traefik deploy. It is added in Plan 2 when services are migrated, to avoid blocking this bootstrap.

- [ ] **Step 3: Create Flux Kustomization for ingress**

Create `flux/ingress.yaml`:

```yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ingress
  namespace: flux-system
spec:
  interval: 10m
  path: ./ingress
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: namespaces
    - name: sealed-secrets
```

- [ ] **Step 4: Commit and push**

```bash
cd ~/homelab-apps
git add ingress/traefik-helmrepo.yaml ingress/traefik-helmrelease.yaml flux/ingress.yaml
git commit -m "feat: deploy Traefik v3 as k3s workload in ingress namespace"
git push
```

- [ ] **Step 5: Verify Traefik is running**

```bash
flux reconcile kustomization ingress --with-source
kubectl wait deployment -n ingress traefik --for=condition=Available --timeout=3m
kubectl get pods -n ingress
```

Expected: Traefik pod `Running`.

- [ ] **Step 6: Verify Traefik is handling port 80/443**

```bash
curl -o /dev/null -sw "%{http_code}" http://localhost/
```

Expected: `404` (Traefik is up, no routes configured yet — that's correct at this stage).

______________________________________________________________________

## Task 15: Deploy cloudflared

- [ ] **Step 1: Create the cloudflared tunnel token secret**

First create a plain Kubernetes Secret manifest (do not commit this file):

```bash
cat > /tmp/cloudflared-secret.yaml << EOF
apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-token
  namespace: ingress
type: Opaque
stringData:
  token: "PASTE_TUNNEL_TOKEN_HERE"
EOF
```

Replace `PASTE_TUNNEL_TOKEN_HERE` with the tunnel token from the Cloudflare dashboard (different from the credentials JSON — this is the newer token-based auth format). If you only have the credentials JSON (`ce54cb73-83b2-4628-8246-26955d280641`), see the note below.

**Note on credentials format:** The existing `sys.services.cloudflared` config uses a credentials file (JSON). The k3s Deployment uses the newer `TUNNEL_TOKEN` env var. In Cloudflare dashboard → Zero Trust → Networks → Tunnels → your tunnel → Edit → choose "Install and run connector" → copy the token. Alternatively keep using the credentials file — update the Deployment in Step 2 to mount a Secret containing the JSON file instead.

- [ ] **Step 2: Seal the secret**

```bash
kubeseal --format yaml < /tmp/cloudflared-secret.yaml \
  > ~/homelab-apps/ingress/cloudflared-secret-sealed.yaml
rm /tmp/cloudflared-secret.yaml
```

- [ ] **Step 3: Create cloudflared Deployment**

Create `ingress/cloudflared-deployment.yaml`:

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared
  namespace: ingress
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflared
  template:
    metadata:
      labels:
        app: cloudflared
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          args:
            - tunnel
            - --no-autoupdate
            - run
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cloudflared-token
                  key: token
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            runAsUser: 65532
```

- [ ] **Step 4: Commit and push**

```bash
cd ~/homelab-apps
git add ingress/cloudflared-deployment.yaml ingress/cloudflared-secret-sealed.yaml
git commit -m "feat: deploy cloudflared tunnel as k3s workload"
git push
```

- [ ] **Step 5: Verify cloudflared connects**

```bash
flux reconcile kustomization ingress --with-source
kubectl wait deployment -n ingress cloudflared --for=condition=Available --timeout=2m
kubectl logs -n ingress deployment/cloudflared --tail=20
```

Expected: logs show `Connection registered` and `Registered tunnel connection` lines.

______________________________________________________________________

## Task 16: Apply default-deny NetworkPolicies and final validation

- [ ] **Step 1: Create default-deny NetworkPolicies**

Create `network/default-deny.yaml`:

```yaml
# Default deny all ingress and egress in every workload namespace.
# Explicit allows are added per-service in Plans 2 and 3.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: vms
spec:
  podSelector: {}
  policyTypes: ["Ingress", "Egress"]
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: media
spec:
  podSelector: {}
  policyTypes: ["Ingress", "Egress"]
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: apps
spec:
  podSelector: {}
  policyTypes: ["Ingress", "Egress"]
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: internal
spec:
  podSelector: {}
  policyTypes: ["Ingress", "Egress"]
---
# Allow all ingress traffic within ingress namespace (Traefik ↔ cloudflared)
# and egress to the cluster for proxying.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-ingress-namespace
  namespace: ingress
spec:
  podSelector: {}
  policyTypes: ["Ingress", "Egress"]
  ingress:
    - {}
  egress:
    - {}
```

- [ ] **Step 2: Commit and push**

```bash
cd ~/homelab-apps
git add network/default-deny.yaml
git commit -m "feat: apply default-deny NetworkPolicies to all workload namespaces"
git push
```

- [ ] **Step 3: Final cluster health check**

```bash
flux reconcile kustomization network --with-source
kubectl get nodes
```

Expected: `blizzard   Ready`.

```bash
kubectl get pods -A | grep -v Running | grep -v Completed | grep -v flux-system
```

Expected: no unexpected non-Running pods (Flux pods themselves may show `Pending` briefly during reconciliation).

```bash
kubectl get helmrelease -A
```

Expected: all HelmReleases show `Ready: True`.

```bash
flux get all -A
```

Expected: all Kustomizations and HelmReleases `Ready`.

- [ ] **Step 4: Verify MicroVMs are still running**

```bash
sudo systemctl list-units 'microvm@*' --state=active
```

Expected: all previously-enabled MicroVMs still `active (running)`. The bootstrap changes nothing about the MicroVM stack.

- [ ] **Step 5: Record completion**

```bash
echo "Plan 1 complete: $(date)" >> ~/homelab-apps/MIGRATION_LOG.md
git add MIGRATION_LOG.md
git commit -m "chore: record Plan 1 completion"
git push
```

______________________________________________________________________

## What's next

**Plan 2 — Container Services:** Migrate all `media`, `apps`, and `internal` namespace workloads (arr stack, Gluetun VPN sidecar, overseerr, searx, mealie, actual, tautulli, flaresolverr). Each service gets a Deployment, Service, PVC, NetworkPolicy allows, and IngressRoute in homelab-apps.

**Plan 3 — VM Migration + Cleanup:** Migrate all 9 KubeVirt VMs (gitea, matrix-synapse, immich, firefly, firefly-importer, paperless, wireguard, firefox, brave), then remove `vms/`, `microvm` flake input, and all MicroVM host config from nix-config.

______________________________________________________________________

## Execution Log — What Actually Happened (2026-05-04/05)

This section documents every deviation from the original plan encountered during the first real execution, so future runs avoid the same pitfalls.

______________________________________________________________________

### Critical bootstrap order correction

**Original plan:** Install Cilium via Flux HelmRelease → bootstrap Flux → Cilium deploys automatically.

**What happened:** All Flux controller pods (helm-controller, kustomize-controller, notification-controller, source-controller) were stuck `Pending` because the node had no CNI. Once manually scheduled after the containerd config issue was fixed, they entered `CrashLoopBackOff` because Cilium hadn't programmed its eBPF service maps yet — pods couldn't reach the Kubernetes API ClusterIP (`10.43.0.1:443`).

**Correct order (mandatory):**

1. k3s starts with `--flannel-backend=none --disable-kube-proxy --disable-network-policy`
1. Install Cilium **directly via Helm** (not through Flux) and wait for `1/1 Running`
1. Verify pod-to-ClusterIP: `kubectl run test --image=curlimages/curl --restart=Never --rm -it -- curl -k https://10.43.0.1:443/healthz` must return `401`, not a timeout
1. **Only then** bootstrap Flux
1. Flux adopts the Cilium release via the HelmRelease manifest

______________________________________________________________________

### NixOS firewall: two required changes

The NixOS firewall dropped all pod traffic by default. Two settings were missing:

**1. Reverse path filter must be disabled:**

```nix
networking.firewall.checkReversePath = false;
```

Strict rp_filter (`= true`, the NixOS default) drops packets from pod interfaces because the source IP (in the pod CIDR `10.42.0.0/16`) doesn't match the expected return path. This caused complete pod network failure — pods had zero connectivity to anything.

**2. Cilium's pod veth interfaces must be trusted:**

```nix
networking.firewall.trustedInterfaces = [ "lxc+" ];
```

Cilium names its host-side veth interfaces `lxcXXXXXXXX` (not `cni*`). The original plan used `"cni+"`, which matched nothing. Without trusting `lxc+`, pod-to-host traffic (needed for pods to reach the Kubernetes API server) was blocked by the INPUT chain.

Both changes require `nixos-rebuild switch` and a Cilium DaemonSet rollout to take effect.

______________________________________________________________________

### k3s `--disable-kube-proxy` is mandatory with Cilium

k3s has a built-in kube-proxy equivalent that manages the `KUBE-SERVICES` iptables chain. With Cilium's `kubeProxyReplacement: true`, both systems try to handle service routing simultaneously — Cilium via eBPF, k3s via iptables. This causes ClusterIP timeouts even after pod-to-internet works.

**Fix added to `modules/virtualisation/k3s.nix`:**

```nix
"--disable-kube-proxy"
```

Without this flag, pods can reach the internet (eBPF masquerade works) but cannot reach any ClusterIP (double-NAT conflict).

______________________________________________________________________

### `kubeProxyReplacement: false` does not work with k3s

Tried `kubeProxyReplacement: false` as a workaround when `true` wasn't working. This makes Cilium defer service routing to kube-proxy — but k3s has no standalone kube-proxy process. Result: complete pod-to-ClusterIP failure. Cilium's BPF intercepts pod traffic to ClusterIPs but has no service maps to forward it.

**`kubeProxyReplacement: true` is the only correct setting for k3s.**

______________________________________________________________________

### Cilium native routing mode, not VXLAN

The initial Cilium install defaulted to VXLAN tunnel mode. The reference implementation (nixos-k3s) uses native routing, which is simpler and more reliable for a single node:

```yaml
routingMode: native
ipv4NativeRoutingCIDR: "10.42.0.0/16"
autoDirectNodeRoutes: true
```

Updated in `homelab-apps/network/cilium-helmrelease.yaml`.

______________________________________________________________________

### Cilium version: 1.16.6 → 1.19.3

Cilium 1.16.6 had zero pod connectivity on kernel 6.18.26 — likely eBPF compatibility gaps. Upgrading to 1.19.3 resolved it (in combination with the NixOS firewall fixes above; hard to isolate which was the deciding factor).

______________________________________________________________________

### gVisor containerd config broke all pod sandboxes

The gVisor systemd service wrote a `config.toml.tmpl` to `/var/lib/rancher/k3s/agent/etc/containerd/` that replaced k3s's entire containerd config instead of extending it. k3s uses Go templates with variables like `{{ .NodeConfig.AgentConfig.CNIBinDir }}` that our minimal TOML completely omitted. Result: containerd couldn't create any pod sandbox (`FailedCreatePodSandBox: rpc error: code = InvalidArgument`), blocking even the Cilium DaemonSet pods from starting.

**Fix:** Delete the broken template file before reinstalling Cilium:

```bash
sudo rm /var/lib/rancher/k3s/agent/etc/containerd/config.toml.tmpl
sudo systemctl restart k3s
```

The `gvisor.nix` approach needs to be redesigned to use k3s's proper template extension format (`{{ template "base" . }}` as the first line). This is deferred — gVisor is not yet functional on this cluster.

______________________________________________________________________

### GitHub fine-grained token needs Administration permission for Flux bootstrap

`flux bootstrap github` requires write access to repository deploy keys (GitHub API `repos/{owner}/{repo}/keys`). A fine-grained token with only `contents: rw` is insufficient. Add **Administration: Read and Write** to the token.

______________________________________________________________________

### `flux` CLI is not installed system-wide

Use `nix run nixpkgs#fluxcd -- <args>` anywhere a `flux` command is needed, or add `pkgs.fluxcd` to blizzard's system packages in nix-config.

______________________________________________________________________

### After `nixos-rebuild switch`, restart k3s and wait for Cilium

`nixos-rebuild switch` restarts k3s when k3s-related options change (flags, modules). This terminates all pods including Cilium. After the switch:

1. Run `sudo systemctl restart k3s` if pods are in a stale state
1. Wait for `kubectl get pods -n kube-system | grep cilium` to show `1/1 Running`
1. Verify pod connectivity before proceeding

______________________________________________________________________

### Cilium Helm values: final working configuration

```yaml
kubeProxyReplacement: true
k8sServiceHost: "127.0.0.1"      # Cilium bootstraps via localhost, not ClusterIP
k8sServicePort: 6443
ipam:
  mode: kubernetes
routingMode: native
ipv4NativeRoutingCIDR: "10.42.0.0/16"
autoDirectNodeRoutes: true
hubble:
  relay:
    enabled: true
  ui:
    enabled: true
operator:
  replicas: 1
```

Install command (replace `1.19.3` with latest stable):

```bash
nix run nixpkgs#kubernetes-helm -- install cilium cilium \
  --repo https://helm.cilium.io \
  --namespace kube-system \
  --version 1.19.3 \
  --set kubeProxyReplacement=true \
  --set k8sServiceHost=127.0.0.1 \
  --set k8sServicePort=6443 \
  --set ipam.mode=kubernetes \
  --set routingMode=native \
  --set ipv4NativeRoutingCIDR=10.42.0.0/16 \
  --set autoDirectNodeRoutes=true \
  --set "hubble.relay.enabled=true" \
  --set "hubble.ui.enabled=true" \
  --set operator.replicas=1
```
